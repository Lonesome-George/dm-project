1.优化预处理代码；
2.优化效率：
目前考虑两种解决方法：
	-快速定位到所要读取的行；
	-每次遇到访问缺失时，只从文件中读取该项及附近的49条记录。

下一步
1.考虑使用索引加快查找速率。
2.针对不同数据使用不同的load_size.比如user_item_dict肯定是顺序读的，可以设load_size值大一点；而item_user_dict和item_item_sim_dice是乱序读的，load_size值太大会读入一些根本不会访问到的项，所以设小一点会提高效率。